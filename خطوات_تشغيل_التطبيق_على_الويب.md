# 🚀 خطوات تشغيل التطبيق على الويب

## ✅ تم إعداد كل شيء! الآن فقط اتبع هذه الخطوات:

---

## 📋 الخطوة 1: تشغيل Backend

### 1.1 افتح Terminal وانتقل للمجلد:
```bash
cd E:\Altayar-app\Altayar-app-final\backend
```

### 1.2 ثبت المكتبات:
```bash
npm install
```

### 1.3 أنشئ ملف .env:
```bash
copy env.example .env
```

### 1.4 افتح ملف `.env` وأضف Firebase API Key:

**كيفية الحصول على Firebase API Key:**
1. اذهب إلى: https://console.firebase.google.com/project/altayar-46d6f/settings/general
2. ابحث عن "Web API Key"
3. انسخه

**أضف في ملف `.env`:**
```env
FIREBASE_API_KEY=your-firebase-api-key-here
```

### 1.5 شغل Migrations:
```bash
npm run migrate:latest
```

### 1.6 شغل Backend:
```bash
npm run dev
```

✅ **تحقق**: افتح المتصفح على `http://localhost:5000/api/health`

---

## 📋 الخطوة 2: بناء Flutter App للويب

### 2.1 افتح Terminal جديد وانتقل للمجلد:
```bash
cd E:\AltayarFlutter\Altayar
```

### 2.2 ثبت المكتبات:
```bash
flutter pub get
```

### 2.3 ابني التطبيق:
```bash
flutter build web --release
```

✅ **التحقق**: الملفات في `build/web/`

---

## 📋 الخطوة 3: نشر التطبيق على Firebase Hosting

### 3.1 ثبت Firebase CLI (مرة واحدة فقط):
```bash
npm install -g firebase-tools
```

### 3.2 سجل الدخول (مرة واحدة فقط):
```bash
firebase login
```

### 3.3 هيئ Firebase Hosting (مرة واحدة فقط):
```bash
firebase init hosting
```

**عند السؤال، اختر:**
- ✅ Use an existing project: `altayar-46d6f`
- ✅ Public directory: `build/web`
- ✅ Configure as single-page app: `Yes`
- ❌ Set up automatic builds: `No`

### 3.4 انشر التطبيق:
```bash
# تأكد من أنك في مجلد Flutter
cd E:\AltayarFlutter\Altayar

# ابني التطبيق
flutter build web --release

# انشر
firebase deploy --only hosting
```

✅ **النتيجة**: ستحصل على رابط مثل:
- `https://altayar-46d6f.web.app`
- `https://altayar-46d6f.firebaseapp.com`

---

## 🎉 تم! التطبيق يعمل على الويب الآن!

### ما يمكنك فعله الآن:

1. ✅ **افتح الرابط** الذي حصلت عليه من Firebase
2. ✅ **اختبر التطبيق** - يجب أن يعمل بشكل كامل
3. ✅ **اختبر Deep Links** - أنشئ رابط من Backend API
4. ✅ **ربط Domain** - يمكنك ربط domain مخصص من Firebase Console

---

## 🔗 روابط مهمة

- **Firebase Console**: https://console.firebase.google.com/project/altayar-46d6f
- **Dynamic Links**: https://console.firebase.google.com/project/altayar-46d6f/durablelinks
- **Backend API**: http://localhost:5000/api
- **Health Check**: http://localhost:5000/api/health

---

## 🐛 إذا واجهت مشاكل

### Backend لا يعمل؟
- تحقق من PostgreSQL يعمل
- تحقق من ملف `.env` وصحة البيانات
- تحقق من المنفذ 5000 غير مستخدم

### Flutter build فشل؟
```bash
flutter clean
flutter pub get
flutter build web --release
```

### Firebase لا يعمل؟
- تحقق من `FIREBASE_API_KEY` في ملف `.env`
- تحقق من Firebase Console

---

## 📚 للمزيد من التفاصيل

- **`QUICK_START_WEB.md`** - دليل سريع تفصيلي
- **`README_WEB_DEPLOYMENT.md`** - دليل شامل
- **`FIREBASE_DYNAMIC_LINKS_SETUP.md`** - إعداد Firebase

---

## ✨ كل شيء جاهز! فقط شغل الخطوات أعلاه 🎉

